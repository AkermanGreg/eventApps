<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Event Mapper</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if current_user %>
       
        <li><%= link_to "Events", "/events"  %></li>
        <li><%= link_to "Users", "/users"  %></li>
        <% else %>
         <li><%= link_to "Events", "/events"  %></li>
        <% end %> 
      </ul>
  
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">User Options <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">    
              <li><%= link_to "Add Event", "/events/new"  %></li>
              
             
          </ul>
          </li>
         
          <li><a href="#"><%= current_user.email %></a></li>
          <li><%= link_to "Sign out", sessions_path, method: :delete %></li>
          <% else %>

         <li>  <div class="marginRight"><%= button_to "Sign Up", new_user_path, method: :get, :class => 'btn btn-default navbar-btn btn-sm' %></div></li>
         <li><div class="marginRight"><%= button_to "Sign In", new_sessions_path, method: :get, :class => 'btn btn-default navbar-btn btn-sm' %></div></li>

        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>